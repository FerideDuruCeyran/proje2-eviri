@model ExcelUploader.Models.DashboardViewModel
@{
    ViewData["Title"] = "Ana Sayfa";
}

<!-- Test CSS Loading -->
<div class="test-css-loaded">
    🎨 ANA SAYFA CSS YÜKLENDİ! Bu kırmızı kutu görünüyorsa CSS çalışıyor demektir!
</div>

<!-- Test Custom CSS Loading -->
<div class="test-custom-css">
    🚀 ANA SAYFA CUSTOM CSS YÜKLENDİ! Bu gradient kutu görünüyorsa custom CSS çalışıyor demektir!
</div>

<!-- Hero Section -->
<div class="hero-section mb-5">
    <div class="container-fluid">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <h1 class="display-4 fw-bold text-white mb-3">
                    <i class="bi bi-file-earmark-excel me-3"></i>
                    Excel Uploader Dashboard
                </h1>
                <p class="lead text-white-50 mb-4">
                    Excel dosyalarınızı kolayca yükleyin, SQL veritabanına aktarın ve verilerinizi yönetin.
                </p>
                <div class="d-flex gap-3">
                    <a href="@Url.Action("Upload", "Home")" class="btn btn-light btn-lg px-4">
                        <i class="bi bi-cloud-upload me-2"></i>
                        Excel Yükle
                    </a>
                    <a href="@Url.Action("Data", "Home")" class="btn btn-outline-light btn-lg px-4">
                        <i class="bi bi-table me-2"></i>
                        Veri Görüntüle
                    </a>
                </div>
            </div>
            <div class="col-lg-4 text-center">
                <div class="hero-icon">
                    <i class="bi bi-graph-up-arrow text-white" style="font-size: 8rem; opacity: 0.3;"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Statistics Cards -->
<div class="row mb-5 g-4">
    <div class="col-xl-3 col-md-6">
        <div class="stats-card stats-card-primary">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <div class="stats-number">@Model.TotalRecords.ToString("N0")</div>
                    <div class="stats-label">Toplam Kayıt</div>
                    <div class="stats-change text-success">
                        <i class="bi bi-arrow-up"></i> +12% bu ay
                    </div>
                </div>
                <div class="stats-icon">
                    <i class="bi bi-database"></i>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-xl-3 col-md-6">
        <div class="stats-card stats-card-success">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <div class="stats-number">@Model.ProcessedRecords.ToString("N0")</div>
                    <div class="stats-label">İşlenen Kayıt</div>
                    <div class="stats-change text-success">
                        <i class="bi bi-check-circle"></i> Tamamlandı
                    </div>
                </div>
                <div class="stats-icon">
                    <i class="bi bi-check-circle"></i>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-xl-3 col-md-6">
        <div class="stats-card stats-card-warning">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <div class="stats-number">@Model.PendingRecords.ToString("N0")</div>
                    <div class="stats-label">Bekleyen Kayıt</div>
                    <div class="stats-change text-warning">
                        <i class="bi bi-clock"></i> İşleniyor
                    </div>
                </div>
                <div class="stats-icon">
                    <i class="bi bi-clock"></i>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-xl-3 col-md-6">
        <div class="stats-card stats-card-info">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <div class="stats-number">@Model.DynamicTables.Count</div>
                    <div class="stats-label">Toplam Tablo</div>
                    <div class="stats-change text-info">
                        <i class="bi bi-table"></i> Aktif
                    </div>
                </div>
                <div class="stats-icon">
                    <i class="bi bi-table"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Charts Row -->
<div class="row mb-5 g-4">
    <div class="col-lg-8">
        <div class="card chart-card">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">
                        <i class="bi bi-graph-up me-2"></i>
                        Aylık Yükleme İstatistikleri
                    </h5>
                    <div class="btn-group btn-group-sm" role="group">
                        <button type="button" class="btn btn-outline-primary active">7 Gün</button>
                        <button type="button" class="btn btn-outline-primary">30 Gün</button>
                        <button type="button" class="btn btn-outline-primary">90 Gün</button>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="chart-container" style="position: relative; height: 300px;">
                    <canvas id="monthlyChart"></canvas>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-lg-4">
        <div class="card chart-card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="bi bi-pie-chart me-2"></i>
                    İşlem Durumu
                </h5>
            </div>
            <div class="card-body">
                <div class="chart-container" style="position: relative; height: 300px;">
                    <canvas id="paymentChart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Uploads -->
<div class="row mb-5">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">
                        <i class="bi bi-clock-history me-2"></i>
                        Son Yüklenen Dosyalar
                    </h5>
                    <a href="@Url.Action("Index", "DynamicTable")" class="btn btn-outline-primary btn-sm">
                        <i class="bi bi-arrow-right me-1"></i>Tümünü Gör
                    </a>
                </div>
            </div>
            <div class="card-body">
                @if (Model.DynamicTables.Any())
                {
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Tablo Adı</th>
                                    <th>Dosya Adı</th>
                                    <th>Yüklenen</th>
                                    <th>Kayıt Sayısı</th>
                                    <th>Durum</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var table in Model.DynamicTables)
                                {
                                    <tr>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <div class="table-icon me-3">
                                                    <i class="bi bi-table text-primary"></i>
                                                </div>
                                                <div>
                                                    <strong>@table.TableName</strong>
                                                    @if (!string.IsNullOrEmpty(table.Description))
                                                    {
                                                        <br><small class="text-muted">@table.Description</small>
                                                    }
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <i class="bi bi-file-earmark-excel text-success me-2"></i>
                                                <span class="text-truncate" style="max-width: 200px;" title="@table.FileName">
                                                    @table.FileName
                                                </span>
                                            </div>
                                        </td>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <i class="bi bi-person-circle text-info me-2"></i>
                                                <small class="text-muted">
                                                    @table.UploadedBy<br>
                                                    @table.UploadDate.ToString("dd.MM.yyyy HH:mm")
                                                </small>
                                            </div>
                                        </td>
                                        <td>
                                            <span class="badge bg-primary fs-6">@table.RowCount.ToString("N0")</span>
                                        </td>
                                        <td>
                                            @if (table.IsProcessed)
                                            {
                                                <span class="badge bg-success fs-6">
                                                    <i class="bi bi-check-circle me-1"></i> İşlendi
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-warning fs-6">
                                                    <i class="bi bi-clock me-1"></i> Bekliyor
                                                </span>
                                            }
                                        </td>
                                        <td>
                                            <div class="btn-group" role="group">
                                                <a href="@Url.Action("Data", "DynamicTable", new { tableName = table.TableName })" 
                                                   class="btn btn-sm btn-outline-primary" title="Görüntüle">
                                                    <i class="bi bi-eye"></i>
                                                </a>
                                                <a href="@Url.Action("Edit", "DynamicTable", new { id = table.Id })" 
                                                   class="btn btn-sm btn-outline-secondary" title="Düzenle">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                                <button type="button" class="btn btn-sm btn-outline-danger" title="Sil"
                                                        onclick="deleteTable(@table.Id, '@table.TableName')">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <div class="text-center py-5">
                        <div class="empty-state">
                            <i class="bi bi-inbox text-muted mb-3" style="font-size: 4rem;"></i>
                            <h5 class="text-muted">Henüz tablo yüklenmemiş</h5>
                            <p class="text-muted mb-4">İlk Excel dosyanızı yükleyerek başlayın</p>
                            <a href="@Url.Action("Upload", "Home")" class="btn btn-primary btn-lg">
                                <i class="bi bi-cloud-upload me-2"></i>
                                İlk Excel Dosyasını Yükle
                            </a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row mb-5">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="bi bi-lightning me-2"></i>
                    Hızlı İşlemler
                </h5>
            </div>
            <div class="card-body">
                <div class="row g-4">
                    <div class="col-md-3 col-sm-6">
                        <a href="@Url.Action("Upload", "Home")" class="quick-action-card">
                            <div class="quick-action-icon bg-primary">
                                <i class="bi bi-cloud-upload"></i>
                            </div>
                            <h6>Excel Yükle</h6>
                            <p class="text-muted small">Yeni Excel dosyası yükleyin</p>
                        </a>
                    </div>
                    <div class="col-md-3 col-sm-6">
                        <a href="@Url.Action("Data", "Home")" class="quick-action-card">
                            <div class="quick-action-icon bg-success">
                                <i class="bi bi-table"></i>
                            </div>
                            <h6>Veri Görüntüle</h6>
                            <p class="text-muted small">Mevcut verileri inceleyin</p>
                        </a>
                    </div>
                    <div class="col-md-3 col-sm-6">
                        <a href="@Url.Action("Export", "Home")" class="quick-action-card">
                            <div class="quick-action-icon bg-info">
                                <i class="bi bi-download"></i>
                            </div>
                            <h6>Veri Dışa Aktar</h6>
                            <p class="text-muted small">Verileri Excel olarak indirin</p>
                        </a>
                    </div>
                    <div class="col-md-3 col-sm-6">
                        <a href="@Url.Action("Profile", "Account")" class="quick-action-card">
                            <div class="quick-action-icon bg-warning">
                                <i class="bi bi-person"></i>
                            </div>
                            <h6>Profil</h6>
                            <p class="text-muted small">Hesap ayarlarınızı yönetin</p>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- System Status -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="bi bi-activity me-2"></i>
                    Sistem Durumu
                </h5>
            </div>
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-3">
                        <div class="system-status-item">
                            <div class="status-indicator status-online"></div>
                            <div class="status-text">
                                <strong>Veritabanı</strong>
                                <small class="text-success">Çevrimiçi</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="system-status-item">
                            <div class="status-indicator status-online"></div>
                            <div class="status-text">
                                <strong>Dosya Sistemi</strong>
                                <small class="text-success">Aktif</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="system-status-item">
                            <div class="status-indicator status-online"></div>
                            <div class="status-text">
                                <strong>Excel İşleme</strong>
                                <small class="text-success">Çalışıyor</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="system-status-item">
                            <div class="status-indicator status-online"></div>
                            <div class="status-text">
                                <strong>API</strong>
                                <small class="text-success">Aktif</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Monthly Chart
        const monthlyCtx = document.getElementById('monthlyChart').getContext('2d');
        const monthlyChart = new Chart(monthlyCtx, {
            type: 'line',
            data: {
                labels: @Html.Raw(Json.Serialize(Model.MonthlyData.Select(c => c.Month))),
                datasets: [{
                    label: 'Kayıt Sayısı',
                    data: @Html.Raw(Json.Serialize(Model.MonthlyData.Select(c => c.Count))),
                    borderColor: '#0d6efd',
                    backgroundColor: 'rgba(13, 110, 253, 0.1)',
                    tension: 0.4,
                    fill: true,
                    yAxisID: 'y'
                }, {
                    label: 'Hibe Tutarı (₺)',
                    data: @Html.Raw(Json.Serialize(Model.MonthlyData.Select(c => c.Amount))),
                    borderColor: '#198754',
                    backgroundColor: 'rgba(25, 135, 84, 0.1)',
                    tension: 0.4,
                    fill: true,
                    yAxisID: 'y1'
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                interaction: {
                    mode: 'index',
                    intersect: false,
                },
                scales: {
                    y: {
                        type: 'linear',
                        display: true,
                        position: 'left',
                        title: {
                            display: true,
                            text: 'Kayıt Sayısı'
                        }
                    },
                    y1: {
                        type: 'linear',
                        display: true,
                        position: 'right',
                        title: {
                            display: true,
                            text: 'Hibe Tutarı (₺)'
                        },
                        grid: {
                            drawOnChartArea: false,
                        },
                    }
                },
                plugins: {
                    legend: {
                        position: 'top',
                    },
                    tooltip: {
                        backgroundColor: 'rgba(0, 0, 0, 0.8)',
                        titleColor: '#fff',
                        bodyColor: '#fff',
                        borderColor: '#0d6efd',
                        borderWidth: 1
                    }
                }
            }
        });

        // Payment Chart
        const paymentCtx = document.getElementById('paymentChart').getContext('2d');
        const paymentChart = new Chart(paymentCtx, {
            type: 'doughnut',
            data: {
                labels: ['İşlenen', 'Bekleyen'],
                datasets: [{
                    data: [@Model.ProcessedRecords, @Model.PendingRecords],
                    backgroundColor: [
                        '#198754',
                        '#ffc107'
                    ],
                    borderWidth: 0,
                    hoverOffset: 4
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom',
                    },
                    tooltip: {
                        backgroundColor: 'rgba(0, 0, 0, 0.8)',
                        titleColor: '#fff',
                        bodyColor: '#fff',
                        borderColor: '#0d6efd',
                        borderWidth: 1
                    }
                }
            }
        });

        // Delete table function
        function deleteTable(id, tableName) {
            if (confirm(`"${tableName}" tablosunu silmek istediğinizden emin misiniz? Bu işlem geri alınamaz.`)) {
                // Add delete functionality here
                console.log('Deleting table:', id, tableName);
            }
        }

        // Add some interactivity to stats cards
        document.querySelectorAll('.stats-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0)';
            });
        });
    </script>
}
