@model ExcelUploader.Models.UploadViewModel
@{
    ViewData["Title"] = "Excel Yükle";
}

<!-- Test CSS Loading -->
<div class="test-css-loaded">
    🎨 TEST CSS YÜKLENDİ! Bu kırmızı kutu görünüyorsa CSS çalışıyor demektir!
</div>

<!-- Test Custom CSS Loading -->
<div class="test-custom-css">
    🚀 CUSTOM CSS YÜKLENDİ! Bu gradient kutu görünüyorsa custom CSS çalışıyor demektir!
</div>

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-4 border-bottom">
    <h1 class="h2">
        <i class="bi bi-cloud-upload"></i>
        Excel Dosyası Yükle
    </h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <a href="@Url.Action("Index", "Home")" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i>
            Ana Sayfaya Dön
        </a>
    </div>
</div>

<div class="row g-4">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="bi bi-file-earmark-excel me-2"></i>
                    Dosya Yükleme
                </h5>
            </div>
            <div class="card-body">
                <form asp-action="Upload" method="post" enctype="multipart/form-data" id="uploadForm">
                    <div class="upload-area" id="uploadArea">
                        <div class="upload-content text-center">
                            <div class="upload-icon mb-3">
                                <i class="bi bi-cloud-upload text-primary"></i>
                            </div>
                            <h4 class="text-primary mb-2">Excel Dosyasını Buraya Sürükleyin</h4>
                            <p class="text-muted mb-4">veya dosya seçmek için aşağıdaki butona tıklayın</p>
                            
                            <div class="mb-4">
                                <input asp-for="ExcelFile" type="file" class="form-control" 
                                       accept=".xlsx,.xls" 
                                       style="display: none;" 
                                       id="fileInput" />
                                <button type="button" class="btn btn-primary btn-lg px-5" onclick="document.getElementById('fileInput').click()">
                                    <i class="bi bi-folder2-open me-2"></i>
                                    Dosya Seç
                                </button>
                            </div>
                            
                            <div class="upload-info">
                                <div class="row text-center">
                                    <div class="col-md-4">
                                        <i class="bi bi-check-circle text-success mb-2"></i>
                                        <h6>Desteklenen Formatlar</h6>
                                        <small class="text-muted">.xlsx, .xls</small>
                                    </div>
                                    <div class="col-md-4">
                                        <i class="bi bi-shield-check text-info mb-2"></i>
                                        <h6>Güvenli Yükleme</h6>
                                        <small class="text-muted">Virüs taraması</small>
                                    </div>
                                    <div class="col-md-4">
                                        <i class="bi bi-database text-warning mb-2"></i>
                                        <h6>SQL Aktarımı</h6>
                                        <small class="text-muted">Otomatik dönüşüm</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div id="fileInfo" class="mt-4" style="display: none;">
                        <div class="alert alert-info border-0">
                            <div class="d-flex align-items-center">
                                <div class="file-icon me-3">
                                    <i class="bi bi-file-earmark-excel text-success"></i>
                                </div>
                                <div class="flex-grow-1">
                                    <h6 class="mb-1" id="fileName"></h6>
                                    <div class="file-details">
                                        <span class="badge bg-light text-dark me-2">
                                            <i class="bi bi-hdd me-1"></i>
                                            <span id="fileSize"></span>
                                        </span>
                                        <span class="badge bg-light text-dark me-2">
                                            <i class="bi bi-file-earmark me-1"></i>
                                            <span id="fileFormat"></span>
                                        </span>
                                        <span class="badge bg-success">
                                            <i class="bi bi-check-circle me-1"></i>
                                            Geçerli
                                        </span>
                                    </div>
                                </div>
                                <button type="button" class="btn btn-sm btn-outline-danger ms-3" onclick="removeFile()">
                                    <i class="bi bi-x-lg"></i>
                                </button>
                            </div>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Description" class="form-label fw-bold">
                            <i class="bi bi-chat-text me-2"></i>
                            Açıklama (Opsiyonel)
                        </label>
                        <textarea asp-for="Description" class="form-control" rows="3" 
                                  placeholder="Bu Excel dosyası hakkında açıklama ekleyin... (örn: Öğrenci verileri, 2024 yılı)"></textarea>
                        <div class="form-text">
                            <i class="bi bi-info-circle me-1"></i>
                            Açıklama, dosyayı daha sonra tanımlamanıza yardımcı olur.
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <button type="submit" class="btn btn-primary btn-lg px-5" id="uploadBtn" disabled>
                            <i class="bi bi-upload me-2"></i>
                            Dosyayı Yükle ve SQL'e Aktar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="bi bi-info-circle me-2"></i>
                    Yükleme Bilgileri
                </h5>
            </div>
            <div class="card-body">
                <div class="mb-4">
                    <h6 class="fw-bold text-success mb-3">
                        <i class="bi bi-check-circle me-2"></i> 
                        Desteklenen Formatlar
                    </h6>
                    <div class="format-list">
                        <div class="format-item">
                            <i class="bi bi-file-earmark-excel text-success me-2"></i>
                            <span>.xlsx (Excel 2007+)</span>
                        </div>
                        <div class="format-item">
                            <i class="bi bi-file-earmark-excel text-success me-2"></i>
                            <span>.xls (Excel 97-2003)</span>
                        </div>
                    </div>
                </div>

                <div class="mb-4">
                    <h6 class="fw-bold text-primary mb-3">
                        <i class="bi bi-gear me-2"></i> 
                        İşlem Adımları
                    </h6>
                    <div class="process-steps">
                        <div class="step-item">
                            <div class="step-number">1</div>
                            <div class="step-content">
                                <strong>Excel dosyasını seçin</strong>
                                <small class="text-muted d-block">veya sürükleyin</small>
                            </div>
                        </div>
                        <div class="step-item">
                            <div class="step-number">2</div>
                            <div class="step-content">
                                <strong>Dosya otomatik doğrulanır</strong>
                                <small class="text-muted d-block">format ve boyut kontrolü</small>
                            </div>
                        </div>
                        <div class="step-item">
                            <div class="step-number">3</div>
                            <div class="step-content">
                                <strong>Veriler SQL'e aktarılır</strong>
                                <small class="text-muted d-block">otomatik tablo oluşturma</small>
                            </div>
                        </div>
                        <div class="step-item">
                            <div class="step-number">4</div>
                            <div class="step-content">
                                <strong>Dashboard'da görüntülenir</strong>
                                <small class="text-muted d-block">işlem sonucu</small>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mb-4">
                    <h6 class="fw-bold text-warning mb-3">
                        <i class="bi bi-shield-check me-2"></i> 
                        Güvenlik Özellikleri
                    </h6>
                    <ul class="security-list">
                        <li><i class="bi bi-check text-success me-2"></i>Maksimum dosya boyutu: 50MB</li>
                        <li><i class="bi bi-check text-success me-2"></i>Sadece Excel formatları kabul edilir</li>
                        <li><i class="bi bi-check text-success me-2"></i>Virüs taraması yapılır</li>
                        <li><i class="bi bi-check text-success me-2"></i>Veriler güvenli şekilde saklanır</li>
                    </ul>
                </div>

                <div class="alert alert-warning border-0">
                    <i class="bi bi-exclamation-triangle me-2"></i>
                    <strong>Önemli:</strong> Büyük dosyalar işlenirken lütfen bekleyin. 
                    İşlem süresi dosya boyutuna göre değişebilir.
                </div>
            </div>
        </div>

        <!-- Recent Uploads -->
        <div class="card mt-4">
            <div class="card-header">
                <h6 class="mb-0">
                    <i class="bi bi-clock-history me-2"></i>
                    Son Yüklemeler
                </h6>
            </div>
            <div class="card-body">
                <div class="text-center text-muted py-3">
                    <i class="bi bi-inbox mb-2" style="font-size: 2rem;"></i>
                    <p class="mb-0">Henüz yükleme yapılmamış</p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Upload Progress Modal -->
<div class="modal fade" id="uploadProgressModal" tabindex="-1" data-bs-backdrop="static" data-bs-keyboard="false">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">
                    <i class="bi bi-upload me-2"></i>
                    Dosya Yükleniyor
                </h5>
            </div>
            <div class="modal-body text-center py-4">
                <div class="upload-animation mb-4">
                    <div class="spinner-border text-primary" role="status" style="width: 3rem; height: 3rem;">
                        <span class="visually-hidden">Yükleniyor...</span>
                    </div>
                </div>
                <h6 id="uploadStatus" class="mb-3">Dosya işleniyor...</h6>
                <div class="progress mb-3" style="height: 8px;">
                    <div class="progress-bar progress-bar-striped progress-bar-animated bg-primary" 
                         role="progressbar" style="width: 0%" id="uploadProgress"></div>
                </div>
                <small class="text-muted" id="uploadDetails">Lütfen bekleyin...</small>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const fileInput = document.getElementById('fileInput');
        const uploadArea = document.getElementById('uploadArea');
        const fileInfo = document.getElementById('fileInfo');
        const uploadBtn = document.getElementById('uploadBtn');
        const uploadForm = document.getElementById('uploadForm');

        // File input change event
        fileInput.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                displayFileInfo(file);
                validateFile(file);
            }
        });

        // Drag and drop events
        uploadArea.addEventListener('dragover', function(e) {
            e.preventDefault();
            this.classList.add('dragover');
        });

        uploadArea.addEventListener('dragleave', function(e) {
            e.preventDefault();
            this.classList.remove('dragover');
        });

        uploadArea.addEventListener('drop', function(e) {
            e.preventDefault();
            this.classList.remove('dragover');
            
            const files = e.dataTransfer.files;
            if (files.length > 0) {
                fileInput.files = files;
                displayFileInfo(files[0]);
                validateFile(files[0]);
            }
        });

        function displayFileInfo(file) {
            const fileName = document.getElementById('fileName');
            const fileSize = document.getElementById('fileSize');
            const fileFormat = document.getElementById('fileFormat');

            fileName.textContent = file.name;
            fileSize.textContent = formatFileSize(file.size);
            fileFormat.textContent = file.name.split('.').pop().toUpperCase();

            fileInfo.style.display = 'block';
            uploadArea.style.display = 'none';
        }

        function removeFile() {
            fileInput.value = '';
            fileInfo.style.display = 'none';
            uploadArea.style.display = 'block';
            uploadBtn.disabled = true;
        }

        function formatFileSize(bytes) {
            if (bytes === 0) return '0 Bytes';
            const k = 1024;
            const sizes = ['Bytes', 'KB', 'MB', 'GB'];
            const i = Math.floor(Math.log(bytes) / Math.log(k));
            return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
        }

        function validateFile(file) {
            const allowedExtensions = ['.xlsx', '.xls'];
            const maxSize = 50 * 1024 * 1024; // 50MB

            const fileExtension = '.' + file.name.split('.').pop().toLowerCase();
            const isValidExtension = allowedExtensions.includes(fileExtension);
            const isValidSize = file.size <= maxSize;

            if (isValidExtension && isValidSize) {
                uploadBtn.disabled = false;
                uploadBtn.innerHTML = '<i class="bi bi-upload me-2"></i>Dosyayı Yükle ve SQL\'e Aktar';
            } else {
                uploadBtn.disabled = true;
                let errorMessage = '';
                
                if (!isValidExtension) {
                    errorMessage += 'Geçersiz dosya formatı. ';
                }
                if (!isValidSize) {
                    errorMessage += 'Dosya boyutu çok büyük. ';
                }
                
                // Show error with Bootstrap alert
                showError('Hata: ' + errorMessage);
                removeFile();
            }
        }

        function showError(message) {
            const alertDiv = document.createElement('div');
            alertDiv.className = 'alert alert-danger alert-dismissible fade show';
            alertDiv.innerHTML = `
                <i class="bi bi-exclamation-triangle me-2"></i>
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;
            
            const cardBody = uploadForm.closest('.card-body');
            cardBody.insertBefore(alertDiv, uploadForm);
            
            // Auto-remove after 5 seconds
            setTimeout(() => {
                if (alertDiv.parentNode) {
                    alertDiv.remove();
                }
            }, 5000);
        }

        // Form submit
        uploadForm.addEventListener('submit', function(e) {
            if (!fileInput.files[0]) {
                e.preventDefault();
                showError('Lütfen bir dosya seçin.');
                return;
            }

            // Show progress modal
            const modal = new bootstrap.Modal(document.getElementById('uploadProgressModal'));
            modal.show();

            // Simulate progress
            let progress = 0;
            const progressBar = document.getElementById('uploadProgress');
            const uploadStatus = document.getElementById('uploadStatus');
            const uploadDetails = document.getElementById('uploadDetails');

            const progressInterval = setInterval(() => {
                progress += Math.random() * 15;
                if (progress > 90) progress = 90;
                
                progressBar.style.width = progress + '%';
                
                if (progress < 30) {
                    uploadStatus.textContent = 'Dosya yükleniyor...';
                    uploadDetails.textContent = 'Dosya sunucuya aktarılıyor...';
                } else if (progress < 60) {
                    uploadStatus.textContent = 'Excel dosyası işleniyor...';
                    uploadDetails.textContent = 'Veriler okunuyor ve doğrulanıyor...';
                } else if (progress < 90) {
                    uploadStatus.textContent = 'Veritabanına aktarılıyor...';
                    uploadDetails.textContent = 'SQL veritabanına kayıtlar ekleniyor...';
                }
            }, 200);

            // Store interval ID to clear later
            uploadForm.dataset.progressInterval = progressInterval;
        });
    </script>
}
